apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: greetingservice
spec:
  ports:
    - name: http
      port: 8180
      nodePort: 30080
      targetPort: keycloak-http
  selector:
    app: keycloak
  type: NodePort